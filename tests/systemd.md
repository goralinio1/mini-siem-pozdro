# Autostart
Po restarcie sprawdzić `systemctl status mini-siem-server`, `grafana-server` i `mini-siem-agent`.

Restart obu VM; następnie dwukrotnie `/summary` w odstępie ok. 10 s. `total_logs` powinien rosnąć.
